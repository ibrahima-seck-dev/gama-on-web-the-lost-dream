// BaseScene.js
export default class BaseScene {
    constructor(engine, canvas) {
      this.engine = engine;
      this.canvas = canvas;
      this.scene = null; // L'instance de la scène Babylon.js
    }
  
   
  }
  